meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: {{URL}}/{{API}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test1@example.com",
    "password": "123456"
  }
}
